rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Ultra-permissive rules for debugging HTTP 412 errors
    match /{allPaths=**} {
      allow read, write: if true; // Allow everything for debugging
    }
  }
}